import { ReactQueryProvider } from '@/common/components/provider/ReactQueryProvider';
import { NuqsAdapterProvider } from '@/common/components/provider/NuqsAdapterProvider';

const Provider = {
  ReactQuery: ReactQueryProvider,
  NuqsAdapter: NuqsAdapterProvider,
};

export default Provider;
